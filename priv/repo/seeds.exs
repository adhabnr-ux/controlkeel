# Script for optional local database initialization. You can run it as:
#
#     mix run priv/repo/seeds.exs
#
# ControlKeel does not seed synthetic workspaces, findings, or sessions by default.
# Create real governed records through the onboarding flow, CLI, or MCP tools so
# local state reflects operator intent rather than preloaded training data.

IO.puts("Seeds complete: no default records inserted.")
